# P1 Verified Recovery Checkpoint

## Authoritative State

- Worktree: C:/Users/leile/Documents/Codex/2026-05-06/image-crawler-system-recovered
- Branch: codex/distributed-crawler-v1-delivery
- P0 base: 03d5fa691cafc414d368ac0283c2e329ab240c64
- Installer checkpoint: 3a1e01a600551a631a29f59e4bc79516a4255baa
- HTTP/Worker/Python checkpoint: 155a133
- Persistent UI/CLI flow checkpoint: f9b6f54bbb823a0636c71a51d422a00cc3b7aa93
- Draft PR: https://github.com/leilei6878/image-crawler-system/pull/7
- main fetched and verified at 66aa9c7a45b5cdf275584c564e759190baecbb7b.
- Old 2026-05-02 checkout remains untouched.
- One scoped stash, codex-installer-checkpoint-backup, is retained. It contains
  the already published installer files; their differences were line endings.

## Running Local Services

- Crawler page/API: http://127.0.0.1:3100/social
- PostgreSQL 16.15: 127.0.0.1:55432, database crawler_v1.
- One real generic Worker registered and heartbeating.
- Own process identities: ignored data/local-processes.json.
- Generated credentials: ignored data/local-db.json and data/local-auth.json.
  Do not print, commit or send their contents to external services.
- Normal local runtime rejects private crawl targets. Only isolated integration
  test processes set ALLOW_LOCAL_CRAWL_TARGETS=true.
- The user's debug center remains on 3000 and was never stopped/reconfigured.
- Stop/restart only with scripts/local_runtime.ps1 and scripts/local_database.ps1;
  see LOCAL_RUNTIME.md. Do not kill by process name or port.

## Evidence

- Python: 26 passed with .venv/Scripts/python.exe -m pytest -q -p no:cacheprovider.
- Server: 11 passed with node --test --test-isolation=none test/*.test.js.
- Worker: 5 passed with the same Node command in worker.
- Vite build passed, 95 modules.
- Installer check: isolated actual bat invocations for npm exit 42,
  Playwright exit 43, node --check exit 44 all record failed and return 1.
- Real-flow API check passed with PostgreSQL, real Worker/Python and local HTML.
- BROWSER_CHECK=true node server/test/real-flow.cjs passed:
  page creates source/job, queues run, displays four deduplicated assets;
  image saving, repeated migrations and HTTP listener restart retention.
- Latest browser evidence (ignored):
  data/e2e/crawler_test_1790079973014_0ba741ce/desktop.png
  data/e2e/crawler_test_1790079973014_0ba741ce/mobile.png
- Desktop 1440x1000 and mobile 390x844 inspected. Mobile overflow initially
  failed, was fixed, and the same assertion then passed.

## Failures Resolved

- Installer verifier incorrectly accepted an old failed status after log write
  denial. It now uses isolated paths and verifies the exact native exit.
- Node test subprocess/Vite esbuild restrictions: normal current-user external
  commands work. Node 24 also supports --test-isolation=none for unit tests.
- Worker lacked declared dependencies; installed locally and committed a lockfile.
- Downloaded PostgreSQL includes vendor Python tests; pytest now explicitly
  collects the repository tests/ directory, without changing assertions.
- PostgreSQL sandbox restricted-token startup failed. Current-user external
  pg_ctl start succeeded without UAC, administrator privileges or a Windows service.
- Local .git writes require current-user external execution. Normal git fetch,
  commit and push now work; no further alternate object-store workaround is needed.

## Next Work: P2, Not Final Delivery

Do not mark the goal complete. Existing success tests cover P1 only.

1. Add per-run task/image associations, exact run counts and persistent cursor.
   Current run queries attach all job images to each historical run.
2. Add durable retry eligibility/limited exponential backoff and correct max_retry
   lookup. Current task report reads max_retry from a task row without that field.
3. Validate claim/report/cancel/recovery lock ordering across two Workers.
   Current host-level lock alone does not bound a job across different hosts;
   claimed tasks also remain assigned to an interrupted host.
4. Move source rate accounting into durable shared state and enforce source
   disable and URL edits for already created jobs.
5. Ensure deadline and max image limits are enforced server-side.
6. Exercise real two-Worker contention, stale leases, idempotent duplicate reports,
   429/5xx/timeouts/policy rejection/cancel/retry exhaustion and automatic intervals.
7. Rehearse database process restart and backup restoration; complete bounded
   soak metrics and update the draft PR only after the full acceptance audit.

No existing process/test command from this checkpoint is waiting on a tool
session. Application services were deliberately left available for local use.
